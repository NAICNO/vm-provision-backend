# Terraform Runner Job

Kubernetes Job container based on Terraform that executes infrastructure operations and streams JSON output to RabbitMQ via `send_to_rabbitmq.py`. See the [root README](../README.md) for architecture details.
