apiVersion: apps/v1
kind: Deployment
metadata:
  name: provision-request-queue-consumer
spec:
  replicas: 1
  selector:
    matchLabels:
      app: provision-request-queue-consumer
  template:
    metadata:
      labels:
        app: provision-request-queue-consumer
    spec:
      containers:
        - name: provision-request-queue-consumer
          image: europe-north1-docker.pkg.dev/usit-itf-naic-project/vm-provisioning-docker-dev/provision-request-queue-consumer:latest
          env:
            - name: RABBITMQ_USER
              valueFrom:
                secretKeyRef:
                  name: rabbitmq-secret
                  key: username
            - name: RABBITMQ_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: rabbitmq-secret
                  key: password
          volumeMounts:
            - name: terraform-volume
              mountPath: /data
      volumes:
        - name: terraform-volume
          persistentVolumeClaim:
            claimName: terraform-wd-pvc
      serviceAccountName: rabbitmq-consumer-sa
